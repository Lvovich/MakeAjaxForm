v. 4.3
- Enhancement FIX: добавлена возможность изменить время жизни запроса через параметр "waitingtime". По-умолчанию теперь
  запрос живет 3600 сек.
- В пример добавлено указание нового параметра.

v. 4.2
- Enhancement FIX: в объекте collectedData теперь возвращаются исходные объекты собранных полей ввода.
- Зафиксированы имена свойств объектов collectedData.
- Исправление процедуры сборки: перед минификацией gcc не происходило объединение модулей.
- В пример добавлен вывод в консоль объекта собранных данных.

v. 4.1
- BUG FIX: теперь пустые значения тоже отправляются на сервер.
- Из примера удалено сообщение при начале отправки.
- Из исходников удалены T0D0.
- Изменен README.

v. 4.0
- Изменен триггер onBeforeExchange на onDataCollected.
- Enhancement FIX: добавлен триггер onStartExchange - действия непосредственно перед отправкой данных, после валидации.
  Например, можно включить крутилку на блоке, показывающую, что пошел процесс обмена.
- Удалены предупреждения консоли при отсутствии в параметрах вызова любого триггера.
- Изменен файл описания на обложку репозитория.
- Изменен пример: включена демнострация нового триггера.

v. 3.3
- Приведена шапка релиза к стандарту JSDoc.
- Добавлено исключение элементов формы из отправляемых данных, если у них установлен флаг disabled.
- Изменен сборщик фронтенда: при сборке dev генерируемый файл будет иметь такое-же название, как и минифицированный,
  чтобы не менять подгружаемый файл в примере.

v. 3.2
- Возврат к использованию прототипа. Чтобы в другом проекте можно было перенести конструктор MakeAjaxForm из window.
- Удалена обертка результата в настройках минификации за ненадобностью.
- Вспомогательные функции для объекта actions вынесены из объекта в замыкание, дабы не плодились вместе с объектами.
- Исправлен вывод сообщений об ошибках при инициализации.
- Уточнение этого файла истории версий.

v. 3.1
- BUG FIX: в настройках минификации отсутствовала закрывающая ";", из-за этого при сборке js в другом проекте возникали
  ошибки.
- Упрощена обертка, автоматически добавляемая минификатором.

v. 3.0
- Контейнер и кнопка отправки теперь передаются не селектором, а сразу элементом.
- Подготовка параметров вынесена из-под анонимной функции, чтобы можно было сразу вернуть ошибку создания объекта MAF
  при неправильных контейнере или сабмите.
- Исправление примера согласно новым правилам.
- Сокращена нумерация версий (удален первый разряд). Не будет столько вариантов)
- В шапке релиза теперь будет информация о версии.

v. 0.2.5
- Изменение процедуры минификации: теперь все делает gulp (через пакет google-closure-compiler).
- Исправление структуры модулей - отказ от использования прототипа, чтобы методы не дублировались в разных объектах,
  модули обернуты в анонимную функцию.

v. 0.2.4
- BUG FIX: в минифицированном файле в функцию onBeforeExchange возвращался объект с нечитаемым именем validationErrors.
- BUG FIX: ошибки формата в этом файле истории версий.
- BUG FIX: ошибки описаний функций JSDoc.

v. 0.2.3
- Добавлена проверка на некорретный вызов функции плагина (без new).
- BUG FIX: при ошибке "parameters is invalid" возвращался частичный объект MakeAjaxForm вместо null. Теперь при всех
  ошибках возвращается пустой объект.
- Рефакторинг.
- Изменение процесса сборки: в релизе теперь коммент про авторство и репо.

v. 0.2.2
- BUG FIX: результат валидации возвращался до результата пользовательской функции onBeforeExchange.

v. 0.2.1
- BUG FIX: в минифицированный файл не попало изменение по onExchangeError.
- Добавлена поддержка отправки файлов через input type="file". Без поддержки multiple (отправляется только первый файл).
  При наличии required и отсутствии файла генерируется ошибка валидации.

v. 0.2.0
- Для создания аякс-формы надо создавать объект вместо простого вызова функции (new MakeAjaxForm). Это на случай, когда
  на странице необходимо несколько форм.
- Все параметры перенесены в один объект (объединены opts и funcs).
- Метод отправки запросов теперь только post, кодировка - только multipart/form-data (не надо в атрибутах контейнера
  указывать эти данные).
- onExchangeError предоставляет в параметре ({string} 'no_valid_data') при отсутствии ошибок валидации и данных.
- Ошибки валидации разделены на 2 типа: 'required_wo_value', 'required_wo_check'. Расширены условия валидации для input
  type="checkbox || radio". Теперь они могут быть required.

v. 0.1.5
- Стильные правки.
- Изменено условие проверки данных перед отправкой: пустой массив мог уйти на сервер.
- Добавлено выполнение функции onExchangeError при отсутствии ошибок валидации и данных.

v 0.1.4
- Название функции onExchangeBefore заменил на onBeforeExchange.

v 0.1.3
- Добавлена проверка наличия атрибута name у элементов ввода. Если его нет или пустой, значение этого элемента не
  отправляется.
- Добавлена обработка textarea.

v 0.1.2
- Изменен прием параметров: отдельный объект опций, отдельный объект функций.
- Добавлена функция onExchangeBefore - действия перед отправкой данных на сервер. Если возвращает false, отправка не
  происходит.

v 0.1.1
- Стильные правки.
- Добавлена проверка/подготовка параметров. Теперь их прямое использование (и неуказание при создании формы) безопасно.
- Bug fix: errorsProcess не возвращал false.
- onclick на senderElement реализован через addEventListener, чтобы не перезатирал пользовательские события.

v 0.1.0
- Стильные правки.
- Проверка существования html-элементов, переданных в параметрах вызова. Выход если не существуют.
- Создание объекта ошибки вынесено в отдельную функцию.
- Функция реакции на ошибки валидации выесена в параметры - теперь ее пишет пользователь, поскольку разные требования в
  каждом случае.

v 0.1
- Стильные правки.
- Bug fix: при отсутствии оригинальных стилей и функции onclick на ошибочном элементе, и при нескольких последовательных
  кликах отправки формы, с элемента не убирались стили ошибки.
- Bug fix: при нескольких последовательных отправках с ошибками, не возвращались оригинальные стили и функции при клике
  на ошибочный input.
- Добавлена реакция на незаполненные input'ы типа text, у которых есть атрибут required - отмена отправки данных и
  подсветка input'ов.

v.0
- Решил зафигачить js-плагин для удобного обмена данными ajax-ом. Заодно чтобы формы не было, как таковой, на странице -
  защита от спам-ботов. Идея в том, что плагину передается класс или id контейнера input'ов и класс или id отправляющего
  элемента. По клику на отправляющий элемент, плагин собирает данные всех input'ов в контейнере и отправляет ajax-ом на
  адрес (тоже указывается в параметрах).